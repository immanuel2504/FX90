# set_mode — test scenarios

| | |
|---|---|
| MQTT command | `set_mode` (publish envelope from `mqtt/`) |
| REST endpoint | `PUT /cloud/mode` (send body from `rest/`) |
| Verify with | matching `get_` command / GET endpoint after every test |

| # | File | Expect | Why / what breaks | Analogy (you are the user) | Verify |
|---|---|---|---|---|---|
| 01 | `mqtt/01_valid_full.json` · `rest/01_valid_full.json` | **ACCEPTED** | The schema's own documented example. The golden path. | Like handing in a perfectly filled-out form - the clerk stamps it and files it. If the reader rejects THIS one, the reader (or the schema) has a bug. | success + setting really applied (read back) + command_id echoed |
| 02 | `mqtt/02_valid_minimal.json` · `rest/02_valid_minimal.json` | **ACCEPTED** | Only the required fields (type); every optional field omitted. | Like a form where you only filled the mandatory boxes and skipped the optional ones - still perfectly acceptable. | success + setting really applied (read back) + command_id echoed |
| 04 | `mqtt/04_missing_required_type.json` · `rest/04_missing_required_type.json` | **REJECTED** | Required field 'type' removed. Reader must fail with a clear message naming the field. | Like submitting a passport application without writing your name - the office cannot even start processing it and hands it straight back. | failure + field named + command_id echoed; state unchanged (read back) |
| 05 | `mqtt/05_wrong_type_type.json` · `rest/05_wrong_type_type.json` | **REJECTED** | 'type' should be string but a int was sent. | Like writing the word 'blue' in the phone-number box - right form, wrong kind of answer in that field. | failure + field named + command_id echoed; state unchanged (read back) |
| 06 | `mqtt/06_wrong_type_environment.json` · `rest/06_wrong_type_environment.json` | **REJECTED** | 'environment' should be string but a int was sent. | Like writing the word 'blue' in the phone-number box - right form, wrong kind of answer in that field. | failure + field named + command_id echoed; state unchanged (read back) |
| 07 | `mqtt/07_wrong_type_antennaStopCondition_type.json` · `rest/07_wrong_type_antennaStopCondition_type.json` | **REJECTED** | 'antennaStopCondition.type' should be string but a int was sent. | Like writing the word 'blue' in the phone-number box - right form, wrong kind of answer in that field. | failure + field named + command_id echoed; state unchanged (read back) |
| 08 | `mqtt/08_invalid_enum_type.json` · `rest/08_invalid_enum_type.json` | **REJECTED** | 'type' must be one of ['SIMPLE', 'INVENTORY', 'PORTAL', 'CONVEYOR', 'CUSTOM'] - 'NOT_A_VALID_VALUE' is not on the menu. | Like ordering size 'XXL' in a shop that only stocks S, M and L - the value must come from the fixed menu. | failure + field named + command_id echoed; state unchanged (read back) |
| 09 | `mqtt/09_invalid_enum_environment.json` · `rest/09_invalid_enum_environment.json` | **REJECTED** | 'environment' must be one of ['LOW_INTERFERENCE', 'HIGH_INTERFERENCE', 'VERY_HIGH_INTERFERENCE', 'AUTO_DETECT', 'DEMO'] - 'NOT_A_VALID_VALUE' is not on the menu. | Like ordering size 'XXL' in a shop that only stocks S, M and L - the value must come from the fixed menu. | failure + field named + command_id echoed; state unchanged (read back) |
| 10 | `mqtt/10_unknown_field.json` · `rest/10_unknown_field.json` | **CHECK BEHAVIOUR** | An extra field the schema does not define. Note whether the reader ignores it or rejects it - and that it is consistent across commands. | Like scribbling an extra line onto an official form - a lenient clerk ignores it, a strict one rejects the whole page. Verify which one the reader is, and that it is consistent. | record behaviour; must be consistent across commands |
| 11 | `mqtt/11_empty_payload.json` · `rest/11_empty_payload.json` | **REJECTED (unless all fields optional)** | Empty payload object. | Like handing in a completely blank form - nothing to act on; the reader should say so clearly, not crash or half-apply defaults. | failure + field named + command_id echoed; state unchanged (read back) |
| 12 | `mqtt/12_variant_set_mode.json` · `rest/12_variant_set_mode.json` | **ACCEPTED** | Documented variant 'set_mode' from the official spec examples. | Like a returning customer ordering a different item off the same menu - every documented variant must work, not just the first one. | success + setting really applied (read back) + command_id echoed |
| 13 | `mqtt/13_variant_set_mode_default_FXR90.json` · `rest/13_variant_set_mode_default_FXR90.json` | **ACCEPTED** | Documented variant 'set_mode_default_FXR90' from the official spec examples. | Like a returning customer ordering a different item off the same menu - every documented variant must work, not just the first one. | success + setting really applied (read back) + command_id echoed |
| 14 | `mqtt/14_variant_set_mode_TAG_FOCUS.json` · `rest/14_variant_set_mode_TAG_FOCUS.json` | **ACCEPTED** | Documented variant 'set_mode_TAG_FOCUS' from the official spec examples. | Like a returning customer ordering a different item off the same menu - every documented variant must work, not just the first one. | success + setting really applied (read back) + command_id echoed |
| 15 | `mqtt/15_probe_transmitPower_scalar.json` · `rest/15_probe_transmitPower_scalar.json` | **CHECK BEHAVIOUR** | KNOWN WEAK SPOT: schema says transmitPower is an ARRAY, but the original docs example used a bare number. Send 31.5 and record what firmware does. | Like a known pothole on a road - the map (schema) and the road (firmware) are suspected to disagree here. Drive it carefully and write down what actually happens. | record behaviour; must be consistent across commands |
| 16 | `mqtt/16_probe_transmitPower_array.json` · `rest/16_probe_transmitPower_array.json` | **ACCEPTED** | The schema-correct array form [31.5]. Compare with the scalar probe - exactly one of the two should work. | Like a known pothole on a road - the map (schema) and the road (firmware) are suspected to disagree here. Drive it carefully and write down what actually happens. | success + setting really applied (read back) + command_id echoed |
| 17 | `mqtt/17_invalid_antenna_port_99.json` · `rest/17_invalid_antenna_port_99.json` | **REJECTED** | Antenna port 99 does not exist on an FXR90 (1-4/1-8). | Like setting a kitchen oven to 900 degrees - the dial physically stops far earlier; anything past the limit must be refused, not silently clamped. | failure + field named + command_id echoed; state unchanged (read back) |
| 18 | `mqtt/18_conflict_antennas_power_mismatch.json` · `rest/18_conflict_antennas_power_mismatch.json` | **CHECK BEHAVIOUR** | 4 antennas but only 2 transmit power values - lengths must correspond; reader should reject or document the fill rule. | Like ticking both 'married' and 'single' on the same form - the two answers cannot both be true; the clerk must ask which one you meant, not pick one silently. | record behaviour; must be consistent across commands |
| 19 | `mqtt/19_mqtt_missing_command_id.json` *(MQTT only)* | **REJECTED** | Envelope without command_id - reader must reject (response cannot be correlated). | Like mailing a letter with no return address - even if the reader acts on it, you will never be able to match the reply to your request. | failure + field named + command_id echoed; state unchanged (read back) |
| 20 | `mqtt/20_mqtt_wrong_command_name.json` *(MQTT only)* | **REJECTED / NO RESPONSE** | Command name 'set_mode_TYPO' does not exist. | Like addressing the envelope to a department that does not exist - nobody should pick it up. | failure + field named + command_id echoed; state unchanged (read back) |

**Rejected scenarios:** always read the value back afterwards — a reject that still half-applied the change is the worst bug class.


**Before you start:** read [WHAT_YOU_NEED.md](WHAT_YOU_NEED.md) — equipment, pre-test state, risks and recovery for `set_mode`.
