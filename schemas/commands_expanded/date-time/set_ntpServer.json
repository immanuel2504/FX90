{
  "title": "Set-NTPServer",
  "x-stoplight": {
    "id": "wvmt1ajx1ygwc"
  },
  "type": "object",
  "description": "Sets the NTP Server",
  "examples": [
    {
      "command": "set_ntpServer",
      "command_id": "abcd1324",
      "payload": {
        "server": "time.google.com",
        "server1": "time1.google.com",
        "server2": "time2.google.com"
      }
    }
  ],
  "properties": {
    "command": {
      "type": "string",
      "description": "The value for the key command must be set_ntpServer",
      "enum": [
        "set_ntpServer"
      ]
    },
    "command_id": {
      "type": "string",
      "example": "abcd1324",
      "description": "A unique ID that will be echoed back in the command response by the reader."
    },
    "payload": {
      "title": "set_ntpServer_command",
      "x-stoplight": {
        "id": "7l4j4lf09zlzb"
      },
      "type": "object",
      "properties": {
        "server": {
          "type": "string",
          "description": "NTP server host name or IP address.",
          "example": "time.google.com"
        },
        "server1": {
          "type": "string",
          "description": "Primary NTP server host name or IP address.",
          "example": "time1.google.com"
        },
        "server2": {
          "type": "string",
          "description": "Optional secondary NTP server host name or IP address.",
          "example": "time2.google.com"
        }
      },
      "required": [
        "server"
      ],
      "examples": [
        {
          "server": "time.google.com",
          "server1": "time1.google.com",
          "server2": "time2.google.com"
        }
      ],
      "description": "Sets the reader NTP server configuration."
    }
  },
  "required": [
    "command",
    "command_id",
    "payload"
  ]
}
