{
  "title": "Start RFID Inventory or BLE scan",
  "type": "object",
  "description": "Start RFID inventory, BLE scanning, or both simultaneously.",
  "properties": {
    "command": {
      "type": "string",
      "enum": [
        "start"
      ],
      "description": "Command name. Must be 'start'."
    },
    "command_id": {
      "type": "string",
      "description": "A unique ID that will be echoed back in the command response by the reader."
    },
    "payload": {
      "title": "Start RFID Inventory or BLE scan",
      "type": "object",
      "description": "Start RFID inventory, BLE scanning, or both simultaneously.\n\nThe optional `scanType` array controls which scan types are activated.\n\nOmitting the payload (or `scanType`) defaults to RFID-only.\n\nBLE scanning requires prior BLE configuration via `set_bleConfig`.\n\n`scanType` with \"ble\" cannot be combined with `applyImpinjGen2X`.\n",
      "properties": {
        "scanType": {
          "type": "array",
          "description": "The optional `scanType` array controls which scan types are activated.\n\nPossible values:\n- \"rfid\"\n- \"ble\"\n\nOmitting this field defaults to RFID-only.\n",
          "items": {
            "type": "string",
            "enum": [
              "rfid",
              "ble"
            ]
          }
        },
        "doNotPersistState": {
          "type": "boolean",
          "description": "Set to false to allow the reader to persist the state.\nIf omitted, default behavior applies.\n"
        },
        "applyImpinjGen2X": {
          "type": "boolean",
          "description": "Enables Impinj Gen2X feature.\n\n`scanType` with \"ble\" cannot be combined with this field.\n"
        }
      },
      "examples": [
        {},
        {
          "doNotPersistState": false
        },
        {
          "applyImpinjGen2X": true
        },
        {
          "scanType": [
            "ble"
          ]
        },
        {
          "scanType": [
            "rfid"
          ]
        },
        {
          "scanType": [
            "ble",
            "rfid"
          ]
        }
      ]
    }
  },
  "required": [
    "command",
    "command_id",
    "payload"
  ],
  "examples": [
    {
      "command": "start",
      "command_id": "req-001",
      "payload": {}
    },
    {
      "command": "start",
      "command_id": "req-002",
      "payload": {
        "doNotPersistState": false
      }
    },
    {
      "command": "start",
      "command_id": "req-003",
      "payload": {
        "applyImpinjGen2X": true
      }
    },
    {
      "command": "start",
      "command_id": "req-004",
      "payload": {
        "scanType": [
          "ble"
        ]
      }
    },
    {
      "command": "start",
      "command_id": "req-005",
      "payload": {
        "scanType": [
          "rfid"
        ]
      }
    },
    {
      "command": "start",
      "command_id": "req-006",
      "payload": {
        "scanType": [
          "ble",
          "rfid"
        ]
      }
    }
  ]
}
