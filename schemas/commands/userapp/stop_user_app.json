{
  "title": "Stop User App",
  "x-stoplight": {
    "id": "owiauqz60vzgs"
  },
  "type": "object",
  "description": "Stops the User App if running",
  "properties": {
    "command": {
      "type": "string",
      "enum": [
        "stop_user_app"
      ],
      "example": "stop_user_app",
      "description": "The value for the key command must be stop_user_app"
    },
    "command_id": {
      "type": "string",
      "example": "abcd1324",
      "description": "A unique ID that will be echoed back in the command response by the reader."
    },
    "payload": {
      "$ref": "../../references/request_payload/stop_userapp_command.yaml"
    }
  },
  "required": [
    "command",
    "command_id",
    "payload"
  ]
}
