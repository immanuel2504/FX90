{
  "title": "Get-NTPServer",
  "x-stoplight": {
    "id": "ysey44mkmfcyn"
  },
  "type": "object",
  "description": "Gets the NTP Server",
  "examples": [
    {
      "command": "get_ntpServer",
      "command_id": "abcd1324",
      "payload": {}
    }
  ],
  "properties": {
    "command": {
      "type": "string",
      "example": "get_ntpServer",
      "description": "The value for the key command must be get_ntpServer\n",
      "enum": [
        "get_ntpServer"
      ]
    },
    "command_id": {
      "type": "string",
      "example": "abcd1324",
      "description": "A unique ID that will be echoed back in the command response by the reader."
    },
    "payload": {
      "$ref": "../../references/request_payload/get_ntpServer_command.yaml"
    }
  },
  "required": [
    "command",
    "command_id",
    "payload"
  ]
}
