title: set_startUserapp
type: object
description: start userapp
properties:
  appname:
    type: string
    description: Application name. For REST, the application is identified by the
      appname path parameter; for MQTT, the application is identified by the appname
      payload field.
    example: sample
required:
- appname
