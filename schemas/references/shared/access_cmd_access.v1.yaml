title: access
x-stoplight:
  id: q1exintgft9jj
type: object
properties:
  type:
    type: string
    enum:
    - ACCESS
  config:
    type: object
    properties:
      password:
        type: string
        description: 'Password Field (see EPC Gen2 Spec)

          Must be a hex string with 8 hex characters. '
    required:
    - password
