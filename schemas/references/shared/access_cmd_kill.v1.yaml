title: kill
x-stoplight:
  id: vedcpho6hvsy1
type: object
properties:
  type:
    type: string
    enum:
    - KILL
  config:
    type: object
    required:
    - password
    properties:
      password:
        type: string
        description: 'Password Field (see EPC Gen2 Spec)

          Must be a hex string with 8 hex characters. '
required:
- type
- config
