{
  "title": "Set Region Response",
  "type": "object",
  "description": "Acknowledges region updates.",
  "properties": {
    "command": {
      "type": "string",
      "enum": [
        "set_region"
      ],
      "example": "set_region"
    },
    "command_id": {
      "type": "string",
      "example": "region-set-001"
    },
    "response": {
      "type": "string",
      "enum": [
        "success",
        "failure"
      ],
      "example": "success"
    },
    "payload": {
      "oneOf": [
        {
          "type": "object",
          "additionalProperties": false
        },
        {
          "title": "failure",
          "type": "object",
          "description": "description of the error",
          "examples": [
            {
              "code": 1,
              "message": "invalid payload for version api. expected empty payload"
            }
          ],
          "properties": {
            "code": {
              "type": "integer",
              "description": "The error code",
              "example": 1,
              "enum": [
                1,
                2,
                3,
                6,
                7
              ]
            },
            "message": {
              "type": "string",
              "description": "A brief description of the error"
            }
          },
          "required": [
            "code",
            "message"
          ]
        }
      ]
    }
  },
  "required": [
    "command",
    "command_id",
    "response",
    "payload"
  ],
  "examples": [
    {
      "command": "set_region",
      "command_id": "region-set-001",
      "response": "success",
      "payload": {}
    }
  ]
}
