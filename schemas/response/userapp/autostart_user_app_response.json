{
  "title": "Auto Start User App",
  "x-stoplight": {
    "id": "ketpc00n4npmb"
  },
  "type": "object",
  "description": "Enables/Disables the Auto start for User App",
  "properties": {
    "command": {
      "type": "string",
      "enum": [
        "autostart_user_app"
      ],
      "example": "autostart_user_app",
      "description": "The value for the key command must be autostart_user_app"
    },
    "command_id": {
      "type": "string",
      "example": "abcd1234",
      "description": "A unique ID that will be echoed back in the command response by the reader."
    },
    "response": {
      "type": "string",
      "enum": [
        "success",
        "failure"
      ],
      "example": "success"
    },
    "payload": {
      "oneOf": [
        {
          "$ref": "../../references/response_payload/autostart_userapp_response.yaml"
        },
        {
          "$ref": "../../references/response_payload/errors.yaml"
        }
      ]
    }
  },
  "required": [
    "command",
    "command_id",
    "response"
  ]
}
