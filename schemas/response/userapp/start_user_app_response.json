{
  "title": "Start User App",
  "x-stoplight": {
    "id": "bsmxpgyf12e3p"
  },
  "type": "object",
  "description": "Starts the specified user app",
  "properties": {
    "command": {
      "type": "string",
      "enum": [
        "start_user_app"
      ],
      "example": "start_user_app",
      "description": "The value for the key command must be start_user_app"
    },
    "command_id": {
      "type": "string",
      "example": "abcd1324",
      "description": "A unique ID that will be echoed back in the command response by the reader."
    },
    "response": {
      "type": "string",
      "enum": [
        "success",
        "failure"
      ],
      "example": "success"
    },
    "payload": {
      "oneOf": [
        {
          "$ref": "../../references/response_payload/start_userapp_response.yaml"
        },
        {
          "$ref": "../../references/response_payload/errors.yaml"
        }
      ]
    }
  },
  "required": [
    "command",
    "command_id"
  ]
}
