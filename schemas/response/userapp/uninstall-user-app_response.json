{
  "title": "Uninstall User App",
  "x-stoplight": {
    "id": "3umyko6j84zvn"
  },
  "type": "object",
  "description": "Uninstalls the specified user app",
  "properties": {
    "command": {
      "type": "string",
      "enum": [
        "uninstall-user-app"
      ],
      "example": "uninstall-user-app",
      "description": "The value for the key command must be uninstall-user-app"
    },
    "command_id": {
      "type": "string",
      "example": "abcd134",
      "description": "A unique ID that will be echoed back in the command response by the reader."
    },
    "response": {
      "type": "string",
      "enum": [
        "success",
        "failure"
      ],
      "example": "success"
    },
    "payload": {
      "oneOf": [
        {
          "$ref": "../../references/response_payload/uninstall_userapp_response.yaml"
        },
        {
          "$ref": "../../references/response_payload/errors.yaml"
        }
      ]
    }
  },
  "required": [
    "command",
    "command_id"
  ]
}
