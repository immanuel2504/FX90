{
  "title": "Stop User App",
  "x-stoplight": {
    "id": "kv8h90cf7opf5"
  },
  "type": "object",
  "description": "Stops the User App if running",
  "properties": {
    "command": {
      "type": "string",
      "enum": [
        "set_stopUserapp"
      ],
      "example": "set_stopUserapp",
      "description": "The value for the key command must be set_stopUserapp"
    },
    "command_id": {
      "type": "string",
      "example": "abcd1324",
      "description": "A unique ID that will be echoed back in the command response by the reader."
    },
    "response": {
      "type": "string",
      "enum": [
        "success",
        "failure"
      ],
      "example": "success"
    },
    "payload": {
      "oneOf": [
        {
          "$ref": "../../references/response_payload/stop_userapp_response.yaml"
        },
        {
          "$ref": "../../references/response_payload/errors.yaml"
        }
      ]
    }
  },
  "required": [
    "command",
    "command_id"
  ]
}
