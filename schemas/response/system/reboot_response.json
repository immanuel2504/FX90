{
  "title": "Reboot Reader",
  "x-stoplight": {
    "id": "hl8pidjrohg2x"
  },
  "type": "object",
  "description": "Reboots the reader",
  "properties": {
    "command": {
      "type": "string",
      "enum": [
        "reboot"
      ],
      "example": "reboot",
      "description": "The value for the key command must be reboot"
    },
    "command_id": {
      "type": "string",
      "example": "abcd1431243",
      "description": "A unique ID that will be echoed back in the command response by the reader."
    },
    "response": {
      "type": "string",
      "enum": [
        "success",
        "failure"
      ],
      "example": "success"
    },
    "payload": {
      "oneOf": [
        {
          "$ref": "../../references/response_payload/reboot.yaml"
        },
        {
          "$ref": "../../references/response_payload/errors.yaml"
        }
      ]
    }
  },
  "required": [
    "command",
    "command_id"
  ]
}
