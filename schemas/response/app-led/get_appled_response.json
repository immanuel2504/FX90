{
  "title": "Get App LED",
  "x-stoplight": {
    "id": "nqbba6azh7cuy"
  },
  "type": "object",
  "description": "Retrieves the status of the app LED on the reader",
  "properties": {
    "command": {
      "type": "string",
      "enum": [
        "get_appled"
      ],
      "example": "get_appled",
      "description": "The value for the key command must be get_appled"
    },
    "command_id": {
      "type": "string",
      "description": "A unique ID that will be echoed back in the command response by the reader."
    },
    "response": {
      "type": "string",
      "enum": [
        "success",
        "failure"
      ],
      "example": "success"
    },
    "payload": {
      "oneOf": [
        {
          "$ref": "../../references/response_payload/get_appled_response-2.yaml"
        },
        {
          "$ref": "../../references/response_payload/errors.yaml"
        }
      ]
    }
  },
  "required": [
    "command",
    "command_id",
    "response"
  ]
}
