{
  "title": "Get hostname",
  "x-stoplight": {
    "id": "hmi9t8ucghoqm"
  },
  "type": "object",
  "description": "This command retrieves the hostname information of the reader",
  "examples": [
    {
      "command": "get_hostname",
      "command_id": "a12cb345654",
      "response": "success",
      "payload": {
        "hostName": "FXR90C60C001"
      }
    }
  ],
  "properties": {
    "command": {
      "type": "string",
      "description": "The value for the key command must be get_hostname",
      "enum": [
        "get_hostname"
      ]
    },
    "command_id": {
      "type": "string",
      "description": "A unique ID that will be echoed back in the command response by the reader.",
      "example": "a12cb345654"
    },
    "response": {
      "type": "string",
      "enum": [
        "success",
        "failure"
      ]
    },
    "payload": {
      "oneOf": [
        {
          "$ref": "../../references/response_payload/get_hostName_response.yaml"
        },
        {
          "$ref": "../../references/response_payload/errors.yaml"
        }
      ]
    }
  },
  "required": [
    "command",
    "command_id",
    "response"
  ]
}
