{
  "title": "Set Network Configuration",
  "x-stoplight": {
    "id": "2c3bx4rgn56yn"
  },
  "type": "object",
  "description": "Modifies the reader network configuration",
  "properties": {
    "command": {
      "type": "string",
      "enum": [
        "set_network"
      ],
      "example": "set_network",
      "description": "The value for the key command must be set_network"
    },
    "command_id": {
      "type": "string",
      "example": "abcd345",
      "description": "A unique ID that will be echoed back in the command response by the reader."
    },
    "response": {
      "type": "string",
      "enum": [
        "success",
        "failure"
      ],
      "example": "success"
    },
    "payload": {
      "oneOf": [
        {
          "$ref": "../../references/response_payload/set_network_respnse.yaml"
        },
        {
          "$ref": "../../references/response_payload/errors.yaml"
        }
      ]
    }
  },
  "required": [
    "command",
    "command_id"
  ]
}
