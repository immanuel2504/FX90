{
  "title": "List Installed Certs",
  "x-stoplight": {
    "id": "uqtfat0xwfboq"
  },
  "type": "object",
  "description": "Lists the installed Certs on the reader",
  "properties": {
    "command": {
      "type": "string",
      "enum": [
        "get_certificates"
      ],
      "example": "get_certificates",
      "description": "The value for the key command must be get_certificates"
    },
    "command_id": {
      "type": "string",
      "example": "abcd12354",
      "description": "A unique ID that will be echoed back in the command response by the reader."
    },
    "response": {
      "type": "string",
      "enum": [
        "success",
        "failure"
      ],
      "example": "success"
    },
    "payload": {
      "oneOf": [
        {
          "$ref": "../../references/response_payload/get_installed_certificates_response.yaml"
        },
        {
          "$ref": "../../references/response_payload/errors.yaml"
        }
      ]
    }
  },
  "required": [
    "command",
    "command_id"
  ]
}
