{
  "title": "Refresh certificate",
  "x-stoplight": {
    "id": "v3izmh6pw645t"
  },
  "type": "object",
  "description": "Refresh the existing cert on reader",
  "properties": {
    "command": {
      "type": "string",
      "enum": [
        "set_refreshCertificate"
      ],
      "example": "set_refreshCertificate",
      "description": "The value for the key command must be set_refreshCertificate"
    },
    "command_id": {
      "type": "string",
      "example": "abcd1324",
      "description": "A unique ID that will be echoed back in the command response by the reader."
    },
    "response": {
      "type": "string",
      "enum": [
        "success",
        "failure"
      ],
      "example": "success"
    },
    "payload": {
      "oneOf": [
        {
          "$ref": "../../references/response_payload/refresh_certificate_response.yaml"
        },
        {
          "$ref": "../../references/response_payload/errors.yaml"
        }
      ]
    }
  },
  "required": [
    "command",
    "command_id"
  ]
}
