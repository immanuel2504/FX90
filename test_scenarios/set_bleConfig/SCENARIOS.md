# set_bleConfig — test scenarios

| | |
|---|---|
| MQTT command | `set_bleConfig` (publish envelope from `mqtt/`) |
| REST endpoint | `PUT /cloud/ble-config` (send body from `rest/`) |
| Verify with | matching `get_` command / GET endpoint after every test |

| # | File | Expect | Why / what breaks | Analogy (you are the user) | Verify |
|---|---|---|---|---|---|
| 01 | `mqtt/01_valid_full.json` · `rest/01_valid_full.json` | **ACCEPTED** | The schema's own documented example. The golden path. | Like handing in a perfectly filled-out form - the clerk stamps it and files it. If the reader rejects THIS one, the reader (or the schema) has a bug. | success + setting really applied (read back) + command_id echoed |
| 04 | `mqtt/04_missing_required_ble.json` · `rest/04_missing_required_ble.json` | **REJECTED** | Required field 'ble' removed. Reader must fail with a clear message naming the field. | Like submitting a passport application without writing your name - the office cannot even start processing it and hands it straight back. | failure + field named + command_id echoed; state unchanged (read back) |
| 05 | `mqtt/05_wrong_type_ble_enable.json` · `rest/05_wrong_type_ble_enable.json` | **REJECTED** | 'ble.enable' should be boolean but a str was sent. | Like writing the word 'blue' in the phone-number box - right form, wrong kind of answer in that field. | failure + field named + command_id echoed; state unchanged (read back) |
| 06 | `mqtt/06_unknown_field.json` · `rest/06_unknown_field.json` | **CHECK BEHAVIOUR** | An extra field the schema does not define. Note whether the reader ignores it or rejects it - and that it is consistent across commands. | Like scribbling an extra line onto an official form - a lenient clerk ignores it, a strict one rejects the whole page. Verify which one the reader is, and that it is consistent. | record behaviour; must be consistent across commands |
| 07 | `mqtt/07_empty_payload.json` · `rest/07_empty_payload.json` | **REJECTED (unless all fields optional)** | Empty payload object. | Like handing in a completely blank form - nothing to act on; the reader should say so clearly, not crash or half-apply defaults. | failure + field named + command_id echoed; state unchanged (read back) |
| 08 | `mqtt/08_variant_enable_with_interval.json` · `rest/08_variant_enable_with_interval.json` | **ACCEPTED** | Documented variant 'enable_with_interval' from the official spec examples. | Like a returning customer ordering a different item off the same menu - every documented variant must work, not just the first one. | success + setting really applied (read back) + command_id echoed |
| 09 | `mqtt/09_variant_enable_with_rssi_filter.json` · `rest/09_variant_enable_with_rssi_filter.json` | **ACCEPTED** | Documented variant 'enable_with_rssi_filter' from the official spec examples. | Like a returning customer ordering a different item off the same menu - every documented variant must work, not just the first one. | success + setting really applied (read back) + command_id echoed |
| 10 | `mqtt/10_variant_enable_with_protocols.json` · `rest/10_variant_enable_with_protocols.json` | **ACCEPTED** | Documented variant 'enable_with_protocols' from the official spec examples. | Like a returning customer ordering a different item off the same menu - every documented variant must work, not just the first one. | success + setting really applied (read back) + command_id echoed |
| 11 | `mqtt/11_variant_disable_ble.json` · `rest/11_variant_disable_ble.json` | **ACCEPTED** | Documented variant 'disable_ble' from the official spec examples. | Like a returning customer ordering a different item off the same menu - every documented variant must work, not just the first one. | success + setting really applied (read back) + command_id echoed |
| 12 | `mqtt/12_boundary_rssi_min_-127.json` · `rest/12_boundary_rssi_min_-127.json` | **ACCEPTED** | RSSI filter -127 dBm - BLE RSSI is physically ~-127..0; verify the edge is enforced. | Like handing in a perfectly filled-out form - the clerk stamps it and files it. If the reader rejects THIS one, the reader (or the schema) has a bug. | success + setting really applied (read back) + command_id echoed |
| 13 | `mqtt/13_boundary_rssi_below_range_-200.json` · `rest/13_boundary_rssi_below_range_-200.json` | **REJECTED** | RSSI filter -200 dBm - BLE RSSI is physically ~-127..0; verify the edge is enforced. | Like asking for antenna number 0 when ports start at 1 - one step below the smallest allowed value. | failure + field named + command_id echoed; state unchanged (read back) |
| 14 | `mqtt/14_boundary_rssi_positive_10.json` · `rest/14_boundary_rssi_positive_10.json` | **CHECK BEHAVIOUR** | RSSI filter 10 dBm - BLE RSSI is physically ~-127..0; verify the edge is enforced. | Like a known pothole on a road - the map (schema) and the road (firmware) are suspected to disagree here. Drive it carefully and write down what actually happens. | record behaviour; must be consistent across commands |
| 15 | `mqtt/15_conflict_disabled_with_filters.json` · `rest/15_conflict_disabled_with_filters.json` | **CHECK BEHAVIOUR** | BLE disabled but filters supplied - are the filters stored for later, ignored, or rejected? | Like ticking both 'married' and 'single' on the same form - the two answers cannot both be true; the clerk must ask which one you meant, not pick one silently. | record behaviour; must be consistent across commands |
| 16 | `mqtt/16_mqtt_missing_command_id.json` *(MQTT only)* | **REJECTED** | Envelope without command_id - reader must reject (response cannot be correlated). | Like mailing a letter with no return address - even if the reader acts on it, you will never be able to match the reply to your request. | failure + field named + command_id echoed; state unchanged (read back) |
| 17 | `mqtt/17_mqtt_wrong_command_name.json` *(MQTT only)* | **REJECTED / NO RESPONSE** | Command name 'set_bleConfig_TYPO' does not exist. | Like addressing the envelope to a department that does not exist - nobody should pick it up. | failure + field named + command_id echoed; state unchanged (read back) |

**Rejected scenarios:** always read the value back afterwards — a reject that still half-applied the change is the worst bug class.


**Before you start:** read [WHAT_YOU_NEED.md](WHAT_YOU_NEED.md) — equipment, pre-test state, risks and recovery for `set_bleConfig`.
