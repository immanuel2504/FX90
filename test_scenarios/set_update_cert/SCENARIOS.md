# set_update_cert — test scenarios

| | |
|---|---|
| MQTT command | `set_update_cert` (publish envelope from `mqtt/`) |
| REST endpoint | `PUT /cloud/certificates` (send body from `rest/`) |
| Verify with | matching `get_` command / GET endpoint after every test |

| # | File | Expect | Why / what breaks | Analogy (you are the user) | Verify |
|---|---|---|---|---|---|
| 01 | `mqtt/01_valid_full.json` · `rest/01_valid_full.json` | **ACCEPTED** | The schema's own documented example. The golden path. | Like handing in a perfectly filled-out form - the clerk stamps it and files it. If the reader rejects THIS one, the reader (or the schema) has a bug. | success + setting really applied (read back) + command_id echoed |
| 02 | `mqtt/02_valid_minimal.json` · `rest/02_valid_minimal.json` | **ACCEPTED** | Only the required fields (url, type, name); every optional field omitted. | Like a form where you only filled the mandatory boxes and skipped the optional ones - still perfectly acceptable. | success + setting really applied (read back) + command_id echoed |
| 04 | `mqtt/04_missing_required_url.json` · `rest/04_missing_required_url.json` | **REJECTED** | Required field 'url' removed. Reader must fail with a clear message naming the field. | Like submitting a passport application without writing your name - the office cannot even start processing it and hands it straight back. | failure + field named + command_id echoed; state unchanged (read back) |
| 05 | `mqtt/05_missing_required_type.json` · `rest/05_missing_required_type.json` | **REJECTED** | Required field 'type' removed. Reader must fail with a clear message naming the field. | Like submitting a passport application without writing your name - the office cannot even start processing it and hands it straight back. | failure + field named + command_id echoed; state unchanged (read back) |
| 06 | `mqtt/06_missing_required_name.json` · `rest/06_missing_required_name.json` | **REJECTED** | Required field 'name' removed. Reader must fail with a clear message naming the field. | Like submitting a passport application without writing your name - the office cannot even start processing it and hands it straight back. | failure + field named + command_id echoed; state unchanged (read back) |
| 07 | `mqtt/07_wrong_type_authenticationType.json` · `rest/07_wrong_type_authenticationType.json` | **REJECTED** | 'authenticationType' should be string but a int was sent. | Like writing the word 'blue' in the phone-number box - right form, wrong kind of answer in that field. | failure + field named + command_id echoed; state unchanged (read back) |
| 08 | `mqtt/08_wrong_type_options_username.json` · `rest/08_wrong_type_options_username.json` | **REJECTED** | 'options.username' should be string but a int was sent. | Like writing the word 'blue' in the phone-number box - right form, wrong kind of answer in that field. | failure + field named + command_id echoed; state unchanged (read back) |
| 09 | `mqtt/09_wrong_type_options_password.json` · `rest/09_wrong_type_options_password.json` | **REJECTED** | 'options.password' should be string but a int was sent. | Like writing the word 'blue' in the phone-number box - right form, wrong kind of answer in that field. | failure + field named + command_id echoed; state unchanged (read back) |
| 10 | `mqtt/10_invalid_enum_authenticationType.json` · `rest/10_invalid_enum_authenticationType.json` | **REJECTED** | 'authenticationType' must be one of ['NONE', 'BASIC'] - 'NOT_A_VALID_VALUE' is not on the menu. | Like ordering size 'XXL' in a shop that only stocks S, M and L - the value must come from the fixed menu. | failure + field named + command_id echoed; state unchanged (read back) |
| 11 | `mqtt/11_invalid_enum_type.json` · `rest/11_invalid_enum_type.json` | **REJECTED** | 'type' must be one of ['server', 'client', 'app'] - 'NOT_A_VALID_VALUE' is not on the menu. | Like ordering size 'XXL' in a shop that only stocks S, M and L - the value must come from the fixed menu. | failure + field named + command_id echoed; state unchanged (read back) |
| 12 | `mqtt/12_unknown_field.json` · `rest/12_unknown_field.json` | **CHECK BEHAVIOUR** | An extra field the schema does not define. Note whether the reader ignores it or rejects it - and that it is consistent across commands. | Like scribbling an extra line onto an official form - a lenient clerk ignores it, a strict one rejects the whole page. Verify which one the reader is, and that it is consistent. | record behaviour; must be consistent across commands |
| 13 | `mqtt/13_empty_payload.json` · `rest/13_empty_payload.json` | **REJECTED (unless all fields optional)** | Empty payload object. | Like handing in a completely blank form - nothing to act on; the reader should say so clearly, not crash or half-apply defaults. | failure + field named + command_id echoed; state unchanged (read back) |
| 14 | `mqtt/14_invalid_cert_type.json` · `rest/14_invalid_cert_type.json` | **REJECTED** | 'rootca' is not in the type enum (server, client, app). | Like ordering size 'XXL' in a shop that only stocks S, M and L - the value must come from the fixed menu. | failure + field named + command_id echoed; state unchanged (read back) |
| 15 | `mqtt/15_probe_unreachable_url.json` · `rest/15_probe_unreachable_url.json` | **CHECK BEHAVIOUR** | Certificate URL points to a server that does not exist - verify a clean timeout error (with sensible timeout), not a hang. | Like couriering a parcel to an apartment that does not exist - the failure should say 'no such recipient', not pretend it was delivered. | record behaviour; must be consistent across commands |
| 16 | `mqtt/16_basic_auth_without_options.json` · `rest/16_basic_auth_without_options.json` | **REJECTED** | authenticationType BASIC but no options.username/password supplied. | Like submitting a passport application without writing your name - the office cannot even start processing it and hands it straight back. | failure + field named + command_id echoed; state unchanged (read back) |
| 17 | `mqtt/17_mqtt_missing_command_id.json` *(MQTT only)* | **REJECTED** | Envelope without command_id - reader must reject (response cannot be correlated). | Like mailing a letter with no return address - even if the reader acts on it, you will never be able to match the reply to your request. | failure + field named + command_id echoed; state unchanged (read back) |
| 18 | `mqtt/18_mqtt_wrong_command_name.json` *(MQTT only)* | **REJECTED / NO RESPONSE** | Command name 'set_update_cert_TYPO' does not exist. | Like addressing the envelope to a department that does not exist - nobody should pick it up. | failure + field named + command_id echoed; state unchanged (read back) |

**Rejected scenarios:** always read the value back afterwards — a reject that still half-applied the change is the worst bug class.


**Before you start:** read [WHAT_YOU_NEED.md](WHAT_YOU_NEED.md) — equipment, pre-test state, risks and recovery for `set_update_cert`.
