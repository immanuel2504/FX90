# set_impinjGen2X — test scenarios

| | |
|---|---|
| MQTT command | `set_impinjGen2X` (publish envelope from `mqtt/`) |
| REST endpoint | `PUT /cloud/impinjGen2X` (send body from `rest/`) |
| Verify with | matching `get_` command / GET endpoint after every test |

| # | File | Expect | Why / what breaks | Analogy (you are the user) | Verify |
|---|---|---|---|---|---|
| 01 | `mqtt/01_valid_full.json` · `rest/01_valid_full.json` | **ACCEPTED** | The schema's own documented example. The golden path. | Like handing in a perfectly filled-out form - the clerk stamps it and files it. If the reader rejects THIS one, the reader (or the schema) has a bug. | success + setting really applied (read back) + command_id echoed |
| 04 | `mqtt/04_wrong_type_fastID_enabled.json` · `rest/04_wrong_type_fastID_enabled.json` | **REJECTED** | 'fastID.enabled' should be boolean but a str was sent. | Like writing the word 'blue' in the phone-number box - right form, wrong kind of answer in that field. | failure + field named + command_id echoed; state unchanged (read back) |
| 05 | `mqtt/05_unknown_field.json` · `rest/05_unknown_field.json` | **CHECK BEHAVIOUR** | An extra field the schema does not define. Note whether the reader ignores it or rejects it - and that it is consistent across commands. | Like scribbling an extra line onto an official form - a lenient clerk ignores it, a strict one rejects the whole page. Verify which one the reader is, and that it is consistent. | record behaviour; must be consistent across commands |
| 06 | `mqtt/06_empty_payload.json` · `rest/06_empty_payload.json` | **REJECTED (unless all fields optional)** | Empty payload object. | Like handing in a completely blank form - nothing to act on; the reader should say so clearly, not crash or half-apply defaults. | failure + field named + command_id echoed; state unchanged (read back) |
| 07 | `mqtt/07_variant_disable_fastID.json` · `rest/07_variant_disable_fastID.json` | **ACCEPTED** | Documented variant 'disable_fastID' from the official spec examples. | Like a returning customer ordering a different item off the same menu - every documented variant must work, not just the first one. | success + setting really applied (read back) + command_id echoed |
| 08 | `mqtt/08_variant_protect_tag.json` · `rest/08_variant_protect_tag.json` | **ACCEPTED** | Documented variant 'protect_tag' from the official spec examples. | Like a returning customer ordering a different item off the same menu - every documented variant must work, not just the first one. | success + setting really applied (read back) + command_id echoed |
| 09 | `mqtt/09_variant_unprotect_tag.json` · `rest/09_variant_unprotect_tag.json` | **ACCEPTED** | Documented variant 'unprotect_tag' from the official spec examples. | Like a returning customer ordering a different item off the same menu - every documented variant must work, not just the first one. | success + setting really applied (read back) + command_id echoed |
| 10 | `mqtt/10_variant_enable_protect_read.json` · `rest/10_variant_enable_protect_read.json` | **ACCEPTED** | Documented variant 'enable_protect_read' from the official spec examples. | Like a returning customer ordering a different item off the same menu - every documented variant must work, not just the first one. | success + setting really applied (read back) + command_id echoed |
| 11 | `mqtt/11_variant_disable_protect_read.json` · `rest/11_variant_disable_protect_read.json` | **ACCEPTED** | Documented variant 'disable_protect_read' from the official spec examples. | Like a returning customer ordering a different item off the same menu - every documented variant must work, not just the first one. | success + setting really applied (read back) + command_id echoed |
| 12 | `mqtt/12_variant_enable_tagFocus.json` · `rest/12_variant_enable_tagFocus.json` | **ACCEPTED** | Documented variant 'enable_tagFocus' from the official spec examples. | Like a returning customer ordering a different item off the same menu - every documented variant must work, not just the first one. | success + setting really applied (read back) + command_id echoed |
| 13 | `mqtt/13_variant_disable_tagFocus.json` · `rest/13_variant_disable_tagFocus.json` | **ACCEPTED** | Documented variant 'disable_tagFocus' from the official spec examples. | Like a returning customer ordering a different item off the same menu - every documented variant must work, not just the first one. | success + setting really applied (read back) + command_id echoed |
| 14 | `mqtt/14_variant_quiet_tags.json` · `rest/14_variant_quiet_tags.json` | **ACCEPTED** | Documented variant 'quiet_tags' from the official spec examples. | Like a returning customer ordering a different item off the same menu - every documented variant must work, not just the first one. | success + setting really applied (read back) + command_id echoed |
| 15 | `mqtt/15_variant_unquiet_tags.json` · `rest/15_variant_unquiet_tags.json` | **ACCEPTED** | Documented variant 'unquiet_tags' from the official spec examples. | Like a returning customer ordering a different item off the same menu - every documented variant must work, not just the first one. | success + setting really applied (read back) + command_id echoed |
| 16 | `mqtt/16_variant_advanced_quiet_tags.json` · `rest/16_variant_advanced_quiet_tags.json` | **ACCEPTED** | Documented variant 'advanced_quiet_tags' from the official spec examples. | Like a returning customer ordering a different item off the same menu - every documented variant must work, not just the first one. | success + setting really applied (read back) + command_id echoed |
| 17 | `mqtt/17_variant_advanced_unquiet_tags.json` · `rest/17_variant_advanced_unquiet_tags.json` | **ACCEPTED** | Documented variant 'advanced_unquiet_tags' from the official spec examples. | Like a returning customer ordering a different item off the same menu - every documented variant must work, not just the first one. | success + setting really applied (read back) + command_id echoed |
| 18 | `mqtt/18_combined_multiple_features.json` · `rest/18_combined_multiple_features.json` | **CHECK BEHAVIOUR** | Several Gen2X features toggled in one command - verify combined application is atomic (all or nothing). | Like ticking both 'married' and 'single' on the same form - the two answers cannot both be true; the clerk must ask which one you meant, not pick one silently. | record behaviour; must be consistent across commands |
| 19 | `mqtt/19_mqtt_missing_command_id.json` *(MQTT only)* | **REJECTED** | Envelope without command_id - reader must reject (response cannot be correlated). | Like mailing a letter with no return address - even if the reader acts on it, you will never be able to match the reply to your request. | failure + field named + command_id echoed; state unchanged (read back) |
| 20 | `mqtt/20_mqtt_wrong_command_name.json` *(MQTT only)* | **REJECTED / NO RESPONSE** | Command name 'set_impinjGen2X_TYPO' does not exist. | Like addressing the envelope to a department that does not exist - nobody should pick it up. | failure + field named + command_id echoed; state unchanged (read back) |

**Rejected scenarios:** always read the value back afterwards — a reject that still half-applied the change is the worst bug class.


**Before you start:** read [WHAT_YOU_NEED.md](WHAT_YOU_NEED.md) — equipment, pre-test state, risks and recovery for `set_impinjGen2X`.
