# set_config — test scenarios

| | |
|---|---|
| MQTT command | `set_config` (publish envelope from `mqtt/`) |
| REST endpoint | `PUT /cloud/config` (send body from `rest/`) |
| Verify with | matching `get_` command / GET endpoint after every test |

| # | File | Expect | Why / what breaks | Analogy (you are the user) | Verify |
|---|---|---|---|---|---|
| 01 | `mqtt/01_valid_full.json` · `rest/01_valid_full.json` | **ACCEPTED** | The schema's own documented example. The golden path. | Like handing in a perfectly filled-out form - the clerk stamps it and files it. If the reader rejects THIS one, the reader (or the schema) has a bug. | success + setting really applied (read back) + command_id echoed |
| 04 | `mqtt/04_unknown_field.json` · `rest/04_unknown_field.json` | **CHECK BEHAVIOUR** | An extra field the schema does not define. Note whether the reader ignores it or rejects it - and that it is consistent across commands. | Like scribbling an extra line onto an official form - a lenient clerk ignores it, a strict one rejects the whole page. Verify which one the reader is, and that it is consistent. | record behaviour; must be consistent across commands |
| 05 | `mqtt/05_empty_payload.json` · `rest/05_empty_payload.json` | **REJECTED (unless all fields optional)** | Empty payload object. | Like handing in a completely blank form - nothing to act on; the reader should say so clearly, not crash or half-apply defaults. | failure + field named + command_id echoed; state unchanged (read back) |
| 06 | `mqtt/06_variant_set_config_data_aws.json` · `rest/06_variant_set_config_data_aws.json` | **ACCEPTED** | Documented variant 'set_config_data_aws' from the official spec examples. | Like a returning customer ordering a different item off the same menu - every documented variant must work, not just the first one. | success + setting really applied (read back) + command_id echoed |
| 07 | `mqtt/07_variant_set_config_data_clear.json` · `rest/07_variant_set_config_data_clear.json` | **ACCEPTED** | Documented variant 'set_config_data_clear' from the official spec examples. | Like a returning customer ordering a different item off the same menu - every documented variant must work, not just the first one. | success + setting really applied (read back) + command_id echoed |
| 08 | `mqtt/08_variant_set_config_data_httpPost.json` · `rest/08_variant_set_config_data_httpPost.json` | **ACCEPTED** | Documented variant 'set_config_data_httpPost' from the official spec examples. | Like a returning customer ordering a different item off the same menu - every documented variant must work, not just the first one. | success + setting really applied (read back) + command_id echoed |
| 09 | `mqtt/09_variant_set_config_data_mqtt.json` · `rest/09_variant_set_config_data_mqtt.json` | **ACCEPTED** | Documented variant 'set_config_data_mqtt' from the official spec examples. | Like a returning customer ordering a different item off the same menu - every documented variant must work, not just the first one. | success + setting really applied (read back) + command_id echoed |
| 10 | `mqtt/10_variant_set_config_data_tcp_ip.json` · `rest/10_variant_set_config_data_tcp_ip.json` | **ACCEPTED** | Documented variant 'set_config_data_tcp_ip' from the official spec examples. | Like a returning customer ordering a different item off the same menu - every documented variant must work, not just the first one. | success + setting really applied (read back) + command_id echoed |
| 11 | `mqtt/11_variant_set_config_data_websocket.json` · `rest/11_variant_set_config_data_websocket.json` | **ACCEPTED** | Documented variant 'set_config_data_websocket' from the official spec examples. | Like a returning customer ordering a different item off the same menu - every documented variant must work, not just the first one. | success + setting really applied (read back) + command_id echoed |
| 12 | `mqtt/12_variant_set_config_global_batching.json` · `rest/12_variant_set_config_global_batching.json` | **ACCEPTED** | Documented variant 'set_config_global_batching' from the official spec examples. | Like a returning customer ordering a different item off the same menu - every documented variant must work, not just the first one. | success + setting really applied (read back) + command_id echoed |
| 13 | `mqtt/13_variant_set_config_global_retention.json` · `rest/13_variant_set_config_global_retention.json` | **ACCEPTED** | Documented variant 'set_config_global_retention' from the official spec examples. | Like a returning customer ordering a different item off the same menu - every documented variant must work, not just the first one. | success + setting really applied (read back) + command_id echoed |
| 14 | `mqtt/14_variant_set_config_management_events.json` · `rest/14_variant_set_config_management_events.json` | **ACCEPTED** | Documented variant 'set_config_management_events' from the official spec examples. | Like a returning customer ordering a different item off the same menu - every documented variant must work, not just the first one. | success + setting really applied (read back) + command_id echoed |
| 15 | `mqtt/15_variant_set_config_gpio_led.json` · `rest/15_variant_set_config_gpio_led.json` | **ACCEPTED** | Documented variant 'set_config_gpio_led' from the official spec examples. | Like a returning customer ordering a different item off the same menu - every documented variant must work, not just the first one. | success + setting really applied (read back) + command_id echoed |
| 16 | `mqtt/16_duplicate_connection_names.json` · `rest/16_duplicate_connection_names.json` | **REJECTED** | Two data connections with the same name - schema says names must be unique. | Like two houses on one street with the same number - the postman cannot deliver; duplicates must be refused up front. | failure + field named + command_id echoed; state unchanged (read back) |
| 17 | `mqtt/17_probe_batching_null.json` · `rest/17_probe_batching_null.json` | **CHECK BEHAVIOUR** | KNOWN WEAK SPOT: batching:null is allowed by the MQTT schema via a construct that is invalid OpenAPI 3.0 - verify firmware truly accepts null (= use global batching). | Like a known pothole on a road - the map (schema) and the road (firmware) are suspected to disagree here. Drive it carefully and write down what actually happens. | record behaviour; must be consistent across commands |
| 18 | `mqtt/18_probe_threshold_zero.json` · `rest/18_probe_threshold_zero.json` | **CHECK BEHAVIOUR** | CPU error threshold 0: schema declares no min/max here (docs elsewhere imply 1-99). Record accept/reject/clamp. | Like a known pothole on a road - the map (schema) and the road (firmware) are suspected to disagree here. Drive it carefully and write down what actually happens. | record behaviour; must be consistent across commands |
| 19 | `mqtt/19_probe_threshold_100.json` · `rest/19_probe_threshold_100.json` | **CHECK BEHAVIOUR** | CPU error threshold 100: schema declares no min/max here (docs elsewhere imply 1-99). Record accept/reject/clamp. | Like a known pothole on a road - the map (schema) and the road (firmware) are suspected to disagree here. Drive it carefully and write down what actually happens. | record behaviour; must be consistent across commands |
| 20 | `mqtt/20_probe_threshold_900.json` · `rest/20_probe_threshold_900.json` | **CHECK BEHAVIOUR** | CPU error threshold 900: schema declares no min/max here (docs elsewhere imply 1-99). Record accept/reject/clamp. | Like a known pothole on a road - the map (schema) and the road (firmware) are suspected to disagree here. Drive it carefully and write down what actually happens. | record behaviour; must be consistent across commands |
| 21 | `mqtt/21_mqtt_missing_command_id.json` *(MQTT only)* | **REJECTED** | Envelope without command_id - reader must reject (response cannot be correlated). | Like mailing a letter with no return address - even if the reader acts on it, you will never be able to match the reply to your request. | failure + field named + command_id echoed; state unchanged (read back) |
| 22 | `mqtt/22_mqtt_wrong_command_name.json` *(MQTT only)* | **REJECTED / NO RESPONSE** | Command name 'set_config_TYPO' does not exist. | Like addressing the envelope to a department that does not exist - nobody should pick it up. | failure + field named + command_id echoed; state unchanged (read back) |

**Rejected scenarios:** always read the value back afterwards — a reject that still half-applied the change is the worst bug class.


**Before you start:** read [WHAT_YOU_NEED.md](WHAT_YOU_NEED.md) — equipment, pre-test state, risks and recovery for `set_config`.
