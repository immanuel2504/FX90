# set_network — test scenarios

| | |
|---|---|
| MQTT command | `set_network` (publish envelope from `mqtt/`) |
| REST endpoint | `PUT /cloud/network` (send body from `rest/`) |
| Verify with | matching `get_` command / GET endpoint after every test |

| # | File | Expect | Why / what breaks | Analogy (you are the user) | Verify |
|---|---|---|---|---|---|
| 01 | `mqtt/01_valid_full.json` · `rest/01_valid_full.json` | **ACCEPTED** | The schema's own documented example. The golden path. | Like handing in a perfectly filled-out form - the clerk stamps it and files it. If the reader rejects THIS one, the reader (or the schema) has a bug. | success + setting really applied (read back) + command_id echoed |
| 04 | `mqtt/04_wrong_type_eth0_IPV4_dhcp.json` · `rest/04_wrong_type_eth0_IPV4_dhcp.json` | **REJECTED** | 'eth0.IPV4.dhcp' should be boolean but a str was sent. | Like writing the word 'blue' in the phone-number box - right form, wrong kind of answer in that field. | failure + field named + command_id echoed; state unchanged (read back) |
| 05 | `mqtt/05_wrong_type_eth0_enable.json` · `rest/05_wrong_type_eth0_enable.json` | **REJECTED** | 'eth0.enable' should be boolean but a str was sent. | Like writing the word 'blue' in the phone-number box - right form, wrong kind of answer in that field. | failure + field named + command_id echoed; state unchanged (read back) |
| 06 | `mqtt/06_unknown_field.json` · `rest/06_unknown_field.json` | **CHECK BEHAVIOUR** | An extra field the schema does not define. Note whether the reader ignores it or rejects it - and that it is consistent across commands. | Like scribbling an extra line onto an official form - a lenient clerk ignores it, a strict one rejects the whole page. Verify which one the reader is, and that it is consistent. | record behaviour; must be consistent across commands |
| 07 | `mqtt/07_empty_payload.json` · `rest/07_empty_payload.json` | **REJECTED (unless all fields optional)** | Empty payload object. | Like handing in a completely blank form - nothing to act on; the reader should say so clearly, not crash or half-apply defaults. | failure + field named + command_id echoed; state unchanged (read back) |
| 08 | `mqtt/08_variant_set_Network_ethernet_dhcp.json` · `rest/08_variant_set_Network_ethernet_dhcp.json` | **ACCEPTED** | Documented variant 'set_Network_ethernet_dhcp' from the official spec examples. | Like a returning customer ordering a different item off the same menu - every documented variant must work, not just the first one. | success + setting really applied (read back) + command_id echoed |
| 09 | `mqtt/09_variant_set_Network_ethernet_static.json` · `rest/09_variant_set_Network_ethernet_static.json` | **ACCEPTED** | Documented variant 'set_Network_ethernet_static' from the official spec examples. | Like a returning customer ordering a different item off the same menu - every documented variant must work, not just the first one. | success + setting really applied (read back) + command_id echoed |
| 10 | `mqtt/10_variant_set_Network_ethernet_static_8021x_tls.json` · `rest/10_variant_set_Network_ethernet_static_8021x_tls.json` | **ACCEPTED** | Documented variant 'set_Network_ethernet_static_8021x_tls' from the official spec examples. | Like a returning customer ordering a different item off the same menu - every documented variant must work, not just the first one. | success + setting really applied (read back) + command_id echoed |
| 11 | `mqtt/11_variant_set_Network_ethernet_dhcp_8021x_tls.json` · `rest/11_variant_set_Network_ethernet_dhcp_8021x_tls.json` | **ACCEPTED** | Documented variant 'set_Network_ethernet_dhcp_8021x_tls' from the official spec examples. | Like a returning customer ordering a different item off the same menu - every documented variant must work, not just the first one. | success + setting really applied (read back) + command_id echoed |
| 12 | `mqtt/12_variant_set_Network_ethernet_dhcp_8021x_ttls_mschapv2.json` · `rest/12_variant_set_Network_ethernet_dhcp_8021x_ttls_mschapv2.json` | **ACCEPTED** | Documented variant 'set_Network_ethernet_dhcp_8021x_ttls_mschapv2' from the official spec examples. | Like a returning customer ordering a different item off the same menu - every documented variant must work, not just the first one. | success + setting really applied (read back) + command_id echoed |
| 13 | `mqtt/13_variant_set_Network_ethernet_dhcp_8021x_peap_mschapv2.json` · `rest/13_variant_set_Network_ethernet_dhcp_8021x_peap_mschapv2.json` | **ACCEPTED** | Documented variant 'set_Network_ethernet_dhcp_8021x_peap_mschapv2' from the official spec examples. | Like a returning customer ordering a different item off the same menu - every documented variant must work, not just the first one. | success + setting really applied (read back) + command_id echoed |
| 14 | `mqtt/14_variant_set_Network_wifi_static.json` · `rest/14_variant_set_Network_wifi_static.json` | **ACCEPTED** | Documented variant 'set_Network_wifi_static' from the official spec examples. | Like a returning customer ordering a different item off the same menu - every documented variant must work, not just the first one. | success + setting really applied (read back) + command_id echoed |
| 15 | `mqtt/15_variant_set_Network_wifi_dhcp.json` · `rest/15_variant_set_Network_wifi_dhcp.json` | **ACCEPTED** | Documented variant 'set_Network_wifi_dhcp' from the official spec examples. | Like a returning customer ordering a different item off the same menu - every documented variant must work, not just the first one. | success + setting really applied (read back) + command_id echoed |
| 16 | `mqtt/16_variant_set_Network_wifi_security_wpa2_personal.json` · `rest/16_variant_set_Network_wifi_security_wpa2_personal.json` | **ACCEPTED** | Documented variant 'set_Network_wifi_security_wpa2_personal' from the official spec examples. | Like a returning customer ordering a different item off the same menu - every documented variant must work, not just the first one. | success + setting really applied (read back) + command_id echoed |
| 17 | `mqtt/17_variant_set_Network_wifi_wpa2_enterprise_tls.json` · `rest/17_variant_set_Network_wifi_wpa2_enterprise_tls.json` | **ACCEPTED** | Documented variant 'set_Network_wifi_wpa2_enterprise_tls' from the official spec examples. | Like a returning customer ordering a different item off the same menu - every documented variant must work, not just the first one. | success + setting really applied (read back) + command_id echoed |
| 18 | `mqtt/18_variant_set_Network_wifi_wpa3_enterprise_ttls_mschapv2.json` · `rest/18_variant_set_Network_wifi_wpa3_enterprise_ttls_mschapv2.json` | **ACCEPTED** | Documented variant 'set_Network_wifi_wpa3_enterprise_ttls_mschapv2' from the official spec examples. | Like a returning customer ordering a different item off the same menu - every documented variant must work, not just the first one. | success + setting really applied (read back) + command_id echoed |
| 19 | `mqtt/19_variant_set_Network_wifi_wpa2_enterprise_peap_tls.json` · `rest/19_variant_set_Network_wifi_wpa2_enterprise_peap_tls.json` | **ACCEPTED** | Documented variant 'set_Network_wifi_wpa2_enterprise_peap_tls' from the official spec examples. | Like a returning customer ordering a different item off the same menu - every documented variant must work, not just the first one. | success + setting really applied (read back) + command_id echoed |
| 20 | `mqtt/20_variant_set_Network_wifi_wpa2_enterprise_ttls_tls.json` · `rest/20_variant_set_Network_wifi_wpa2_enterprise_ttls_tls.json` | **ACCEPTED** | Documented variant 'set_Network_wifi_wpa2_enterprise_ttls_tls' from the official spec examples. | Like a returning customer ordering a different item off the same menu - every documented variant must work, not just the first one. | success + setting really applied (read back) + command_id echoed |
| 21 | `mqtt/21_variant_set_Network_wifi_wpa3_enterprise_peap_mschapv2.json` · `rest/21_variant_set_Network_wifi_wpa3_enterprise_peap_mschapv2.json` | **ACCEPTED** | Documented variant 'set_Network_wifi_wpa3_enterprise_peap_mschapv2' from the official spec examples. | Like a returning customer ordering a different item off the same menu - every documented variant must work, not just the first one. | success + setting really applied (read back) + command_id echoed |
| 22 | `mqtt/22_variant_set_Network_bluetooth.json` · `rest/22_variant_set_Network_bluetooth.json` | **ACCEPTED** | Documented variant 'set_Network_bluetooth' from the official spec examples. | Like a returning customer ordering a different item off the same menu - every documented variant must work, not just the first one. | success + setting really applied (read back) + command_id echoed |
| 23 | `mqtt/23_variant_set_Network_wan.json` · `rest/23_variant_set_Network_wan.json` | **ACCEPTED** | Documented variant 'set_Network_wan' from the official spec examples. | Like a returning customer ordering a different item off the same menu - every documented variant must work, not just the first one. | success + setting really applied (read back) + command_id echoed |
| 24 | `mqtt/24_variant_set_Network_hotspot.json` · `rest/24_variant_set_Network_hotspot.json` | **ACCEPTED** | Documented variant 'set_Network_hotspot' from the official spec examples. | Like a returning customer ordering a different item off the same menu - every documented variant must work, not just the first one. | success + setting really applied (read back) + command_id echoed |
| 25 | `mqtt/25_probe_ipv6_prefix_integer.json` · `rest/25_probe_ipv6_prefix_integer.json` | **CHECK BEHAVIOUR** | KNOWN WEAK SPOT: schema says IPv6 prefix is a STRING but every documented example uses integer 64. This is the integer form. | Like a known pothole on a road - the map (schema) and the road (firmware) are suspected to disagree here. Drive it carefully and write down what actually happens. | record behaviour; must be consistent across commands |
| 26 | `mqtt/26_probe_ipv6_prefix_string.json` · `rest/26_probe_ipv6_prefix_string.json` | **CHECK BEHAVIOUR** | The string form '64' of the same prefix. Compare with the integer probe - document which one firmware accepts. | Like a known pothole on a road - the map (schema) and the road (firmware) are suspected to disagree here. Drive it carefully and write down what actually happens. | record behaviour; must be consistent across commands |
| 27 | `mqtt/27_invalid_ip_address.json` · `rest/27_invalid_ip_address.json` | **REJECTED** | 999.999.1.1 is not a valid IPv4 address. | Like writing the word 'blue' in the phone-number box - right form, wrong kind of answer in that field. | failure + field named + command_id echoed; state unchanged (read back) |
| 28 | `mqtt/28_conflict_dhcp_plus_static.json` · `rest/28_conflict_dhcp_plus_static.json` | **CHECK BEHAVIOUR** | dhcp:true while static address/gateway/dns are also supplied - which wins? Must be rejected or clearly documented. | Like ticking both 'married' and 'single' on the same form - the two answers cannot both be true; the clerk must ask which one you meant, not pick one silently. | record behaviour; must be consistent across commands |
| 29 | `mqtt/29_mqtt_missing_command_id.json` *(MQTT only)* | **REJECTED** | Envelope without command_id - reader must reject (response cannot be correlated). | Like mailing a letter with no return address - even if the reader acts on it, you will never be able to match the reply to your request. | failure + field named + command_id echoed; state unchanged (read back) |
| 30 | `mqtt/30_mqtt_wrong_command_name.json` *(MQTT only)* | **REJECTED / NO RESPONSE** | Command name 'set_network_TYPO' does not exist. | Like addressing the envelope to a department that does not exist - nobody should pick it up. | failure + field named + command_id echoed; state unchanged (read back) |

**Rejected scenarios:** always read the value back afterwards — a reject that still half-applied the change is the worst bug class.


**Before you start:** read [WHAT_YOU_NEED.md](WHAT_YOU_NEED.md) — equipment, pre-test state, risks and recovery for `set_network`.
