put:
  tags:
  - userapp
  summary: Trigger reader gateway data delivery
  description: Triggers the reader gateway to process and deliver buffered tag data. No request body.
  operationId: setDataToRG
  responses:
    '200':
      description: OK
      content:
        application/json:
          schema:
            $ref: ../../openapi.yaml#/components/schemas/setDataToRGResponse
  security:
  - bearerAuth: []
