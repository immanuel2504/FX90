get:
  tags:
  - Network
  summary: Gets the read points on the reader
  description: MQTT API :-  get_readPoints <br> Gets the read points on the reader
  operationId: getReadpoints
  responses:
    '200':
      description: OK
      content:
        application/json:
          schema:
            $ref: ../../openapi.yaml#/components/schemas/getReadpointsResponse
          example:
          - '1'
          - '2'
          - '3'
          - '4'
          - '5'
          - '6'
  security:
  - bearerAuth: []
