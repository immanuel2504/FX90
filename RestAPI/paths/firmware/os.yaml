put:
  tags:
  - Firmware
  summary: Updates OS software on device
  description: MQTT API :-  set_os <br> Updates the OS software on the device
  operationId: setOS
  requestBody:
    content:
      application/json:
        schema:
          $ref: ../../openapi.yaml#/components/schemas/setOsRequest
        examples:
          set_os:
            value:
              url: https://10.117.229.9:9093/BUILDS/FXR90_2.0.10.tar.bz2
              authenticationType: NONE
              authenticationOptions:
                username: test
                password: test
  responses:
    '200':
      description: OK
  security:
  - bearerAuth: []
