put:
  tags:
  - Firmware
  summary: Revert Back Firmware Update
  description: MQTT API :-  revertback <br> Reverts the device to the previously installed
    OS version
  operationId: revertBackOS
  requestBody:
    content:
      application/json:
        schema:
          $ref: ../../openapi.yaml#/components/schemas/setRevertbackosRequest
        examples:
          set_revertbackOS:
            value: {}
  responses:
    '200':
      description: OK
  security:
  - bearerAuth: []
