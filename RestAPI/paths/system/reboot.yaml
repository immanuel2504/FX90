put:
  tags:
  - System
  summary: Restarts reader
  description: MQTT API :-  reboot <br> Restarts the reader with the device ID provided
  operationId: reboot
  responses:
    '200':
      description: OK
      content:
        application/json:
          schema:
            $ref: ../../openapi.yaml#/components/schemas/rebootResponse
  security:
  - bearerAuth: []
